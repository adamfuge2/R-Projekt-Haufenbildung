% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-generators.R
\name{generateNoiseData}
\alias{generateNoiseData}
\title{Generate a full (rectangular) space of random (noise) data}
\usage{
generateNoiseData(
  n = 100,
  lower_bounds,
  upper_bounds,
  colnames = paste0("X_", 1:length(lower_bounds)),
  include_cluster = FALSE,
  .print_info = FALSE
)
}
\arguments{
\item{n}{A positive integer. The number of total data points to be
generated.}

\item{lower_bounds, upper_bounds}{numeric vectors with equal length.
Bounds the (multidimensional) rectangle in which the data points will be generated.}

\item{colnames}{A character vector with length matching dim or the length of
the bounds. Default is `'X_1'` to `'X_n'`.}

\item{include_cluster}{A logical of length 1. If \code{TRUE} a column
`'cluster'` will be included in the generated data which will also be
assigned the class `'clustered_data'`. Because this is a noise generator
all data points will be assigned cluster \code{0}.}

\item{.print_info}{A logical of length 1. If \code{TRUE} additional
information will be displayed during runtime. Used in debugging.}
}
\value{
a tibble, every row representing a data point. The columns are named
  using the parameter `colnames`.
}
\description{
Generate a full (rectangular) space of random (noise) data
}
\examples{
generateNoiseData(lower_bounds = c(-1,0),
                  upper_bounds = c(1,1))
generateNoiseData(n = 1000,
                  lower_bounds = c(-180,-90),
                  upper_bounds = c(180,90),
                  colnames = c('longitude','latitude'),
                  .print_info = TRUE)



}
\seealso{
Other cluster data generators: 
\code{\link{generateClusterData}()},
\code{\link{generateClusterDataFromPaths}()}
}
\concept{cluster data generators}
